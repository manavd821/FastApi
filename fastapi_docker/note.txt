docker-compose up --build

# open bash in app container if needed -> container should be in running state
docker exec -it fastapi_container bash

# to run docker container in background inside cli
docker-compose up -d

# to stop docker container 
docker-compose down

# to run fastapi app:
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload